{
  "name": "chess-it",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd Client && npm install && VITE_API_BASE_URL= npm run build && cd ../Server && npm install && mkdir -p public && cp -r ../Client/dist/* public/",
    "start": "cd Server && npm start"
  }
}
